services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: always

  customer:
    build:
      context: ../thambi_customer
      dockerfile: Dockerfile
    ports:
      - "8082:80"
    restart: always
